{
  "scripts": {
    "format": "prettier --write",
    "prettier-watch": "npx onchange \"**/*\" -- prettier --write --ignore-unknown {{changed}}"
  },
  "type": "module",
  "devDependencies": {
    "prettier": "3.6.2"
  }
}
