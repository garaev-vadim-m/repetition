## 🎯 Третий урок - Логические операторы

**Ссылка:** https://learn.javascript.ru/logical-operators

### 🔍 Основные логические операторы:

| Оператор | Название | Описание                                            |
|----------|----------|-----------------------------------------------------|
| `&&`     | И (AND)  | Возвращает `true` если оба операнда истинны         |
| `\|\|`   | ИЛИ (OR) | Возвращает `true` если хотя бы один операнд истинен |
| `!`      | НЕ (NOT) | Возвращает противоположное логическое значение      |

### 🔧 Операторы нулевого слияния и присваивания:

| Оператор | Название              | Описание                                                            | Пример      |
|----------|-----------------------|---------------------------------------------------------------------|-------------|
| `??`     | Нулевого слияния      | Возвращает первый операнд если он не `null/undefined`, иначе второй | `a ?? b`    |
| `??=`    | Нулевого присваивания | Присваивает значение только если переменная `null/undefined`        | `a ??= b`   |
| `&&=`    | И присваивания        | Присваивает значение только если переменная истинна                 | `a &&= b`   |
| `\|\|=`  | ИЛИ присваивания      | Присваивает значение только если переменная ложна                   | `a \|\|= b` |

### 💡 Особенности JavaScript:

- **Ложные значения:** `false`, `0`, `""`, `null`, `undefined`, `NaN`
- **Короткий цикл вычислений:** Операторы прекращают вычисление при достижении результата
- **Возвращают исходные значения:** Могут возвращать не только `boolean`

### 🎯 Практическое применение:

```javascript
// Нулевое слияние
let user = null;
let defaultUser = "Гость";
console.log(user ?? defaultUser); // "Гость"

// Нулевое присваивание
let config = {};
config.theme ??= "dark"; // theme = "dark"
config.theme ??= "light"; // theme остается "dark"

// ИЛИ присваивание
let text = "";
text ||= "Нет текста"; // text = "Нет текста"

// И присваивание
let isValid = true;
isValid &&= checkData(); // Выполняет checkData() только если isValid = true
```
