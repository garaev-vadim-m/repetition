# Алгоритмы и структуры данных в JavaScript

---

## 📦 Array
Упорядоченная коллекция элементов с доступом по индексу.

```javascript
const arr = [10, 23, 45, 78, 91]
```

**Доступ:** O(1) — прямое обращение  
**Поиск:** O(n) — последовательный перебор  
**Вставка/удаление:** O(n) — требуется сдвиг

*Базовый контейнер, основа основ.*

---

## 🔗 Linked List
Последовательность узлов, каждый из которых хранит значение и ссылку на следующий.

```javascript
{ value: 10, next: → } → { value: 23, next: → } → null
```

**Доступ:** O(n) — только через обход  
**Вставка/удаление в начале:** O(1)  
**Память:** дополнительная на хранение ссылок

*Динамическая структура без жёсткой привязки к индексам.*

---

## 🧵 String
Неизменяемая последовательность символов.

```javascript
const text = "алгоритм"
```

**Сравнение:** O(n)  
**Конкатенация:** O(n + m) — создание новой строки  
**Поиск подстроки:** O(n·m) наивный, O(n+m) оптимизированный

*Работа с текстом — отдельная дисциплина.*

---

## 🌳 Binary Tree
Иерархическая структура, где каждый узел имеет не более двух потомков.

```
    10
   /  \
  5    15
 / \     \
3   7     20
```

**Поиск в BST:** O(h), h = высота дерева  
**Вставка/удаление:** O(h)  
**Обходы:** DFS (pre/in/post-order), BFS

*Естественный способ представления иерархий.*

---

## 🗺 HashMap (Object / Map)
Коллекция пар ключ-значение с доступом через хеш-функцию.

```javascript
const map = {
  name: "Алгоритм",
  type: "Сортировка",
  complexity: "O(n log n)"
}
```

**Доступ:** O(1) средний, O(n) в редких случаях коллизий  
**Вставка/удаление:** O(1) средний  
**Порядок:** гарантирован в Map, не гарантирован в Object

*Прямое соответствие без перебора.*

---

## Сравнительная таблица

| Структура    | Доступ | Поиск | Вставка | Удаление |
|--------------|--------|-------|---------|----------|
| Array        | O(1)   | O(n)  | O(n)    | O(n)     |
| Linked List  | O(n)   | O(n)  | O(1)*   | O(1)*    |
| Binary Tree  | O(h)   | O(h)  | O(h)    | O(h)     |
| HashMap      | O(1)   | O(1)  | O(1)    | O(1)     |